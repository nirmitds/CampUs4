services:
  # ── Backend API (Node.js Web Service) ──────────────────────────
  - type: web
    name: campus-backend
    runtime: node
    rootDir: backend
    buildCommand: npm ci
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: MAIL_USER
        sync: false
      - key: MAIL_PASS
        sync: false
      - key: TWILIO_SID
        sync: false
      - key: TWILIO_TOKEN
        sync: false
      - key: TWILIO_FROM
        sync: false
      - key: UPI_ID
        sync: false
      - key: UPI_NAME
        sync: false
      - key: QR_IMAGE_URL
        sync: false
      - key: FRONTEND_URL
        sync: false   # set to your Render static site URL

  # ── Frontend Static Site ────────────────────────────────────────
  - type: web
    name: campus-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false   # set to your backend URL e.g. https://campus-backend.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
